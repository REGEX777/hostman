<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Albums</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center">
    <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-2xl">
        <h1 class="text-3xl font-bold text-center mb-6">Your Albums</h1>
        <ul>
            <% albums.forEach(album => { %>
                <li class="mb-4">
                    <h2 class="text-2xl font-bold mb-2"><%= album.name %> (<%= album.images.length %> images)</h2>
                    <div class="grid grid-cols-3 gap-4">
                        <% album.images.forEach(image => { %>
                            <div class="relative block">
                                <img src="/uploads/<%= image.fileName %>" alt="Image"
                                    class="w-full h-32 object-cover rounded-md border-2 border-gray-600">
                            </div>
                        <% }) %>
                    </div>
                </li>
            <% }) %>
        </ul>
    </div>
</body>
</html>
