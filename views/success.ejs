<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Successful</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white font-sans flex items-center justify-center h-screen m-0">
    <div class="text-center max-w-lg">
        <h1 class="text-6xl font-bold mb-6">Success!</h1>
        <p class="text-xl mb-6">Your files have been uploaded successfully.</p>
        
        <div class="bg-gray-800 p-4 rounded-lg mb-6">
            <h2 class="text-2xl font-semibold mb-4">Uploaded Files:</h2>
            <ul class="list-disc list-inside">
                <% files.forEach(file => { %>
                    <li><%= file.originalname %> (saved as <%= file.filename %>)</li>
                <% }); %>
            </ul>
        </div>
        
        <a href="/" class="inline-block bg-red-900 hover:bg-red-700 text-white font-medium py-2 px-6 rounded">Go to Homepage</a>
    </div>
</body>
</html>
