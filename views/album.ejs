<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= album.name %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-[#161F29] text-white">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-center mb-8"><%= album.name %></h1>

        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
            <% if (album.images && album.images.length > 0) { %>
                <% album.images.forEach(image => { %>
                    <div class="border rounded-lg overflow-hidden shadow-sm hover:shadow-md transition-shadow duration-200">
                        <a href="/post/<%= image.fileName %>"><img src="/uploads/<%= image.fileName %>" alt="Image" class="w-full h-[14rem] object-cover"></a>
                        <% if (user) { %>
                        <div class="p-4">
                                <form action="/favorites/add/<%= image._id %>" method="POST">
                                    <button type="submit" class="w-full bg-blue-500 text-white py-2 rounded hover:bg-blue-600 transition-colors duration-200">Add to Favorites</button>
                                </form>
                        </div>
                        <% } %>

                    </div>
                <% }) %>
            <% } else { %>
                <p class="text-center text-gray-500 col-span-full">No images in this album.</p>
            <% } %>
        </div>
    </div>
</body>
</html>
