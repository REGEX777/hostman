<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Upload</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h2 class="text-2xl font-bold mb-6 text-center">Upload Image</h2>
        <!-- Show the damn error man pls for gods sake -->
        <% //if (//message) { %>
            <div class="text-red-500 mb-4">
                <%= //message %>
            </div>
        <% //} %>

        <form action="/upload" method="POST" enctype="multipart/form-data" class="space-y-6">
            <input type="hidden" name="_csrf" value="<%= //csrfToken %>">
            <div class="mb-4">
                <label class="block mb-2 text-gray-600">Select Image(s)</label>
                <input type="file" name="files" accept="image/*" multiple class="border border-gray-300 rounded-lg p-2 w-full">
            </div>
            <button type="submit" class="w-full bg-indigo-600 text-white py-2 rounded-lg hover:bg-indigo-700">Upload</button>
        </form>
    </div>
</body>
</html>
